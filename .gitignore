# CDN publish build output (regenerated by publish.yml)
dist/

# wrangler / node
worker/node_modules/
.wrangler/

# local conversion binaries (built from the engine; never commit a platform binary)
/bin/
.claude/

# macOS finder metadata
.DS_Store
